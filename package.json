{
  "name": "la-proyeccion-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "La Proyección — prototipo web (banco de pruebas de diseño). El producto final es Unity.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}
